import { FC } from 'react'

const StartPage: FC = () => {
  return (
    <div>
      <h1>Document Analizer</h1>
      <h2>
        Выберите документ в формате docx, нажав на окно ниже, или перетащив искомый файл в это окно
      </h2>
    </div>
  )
}

export default StartPage
